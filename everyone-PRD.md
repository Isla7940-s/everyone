# Everyone · 产品需求文档(PRD)

> **Everyone —— 每个人的分身,替每个人做事。**

| 项 | 内容 |
| --- | --- |
| 版本 | v0.1(黑客松定版) |
| 日期 | 2026-08-27 |
| 状态 | 已与发起人对齐,可直接开发 |
| 读者 | 参与本项目的所有人类与 AI 执行者 |
| 赛事 | 飞书 AI 绝活大会线上黑客松,截稿 2026-09-04 18:00 |
| 目标 | 主攻「绝活之王」(四维:真实价值 30 / 完成度 30 / 飞书+AI 20 / 可复用 20 + 社区投票),叙事兼顾「AI 使唤大师」「最佳飞书 AI Native 实践」 |

---

## 0. 给 AI 执行者的约定(必读)

本文档是本项目的**唯一事实源**。以目标模式 / 自主模式运行时:

1. **不要就本文已覆盖的问题询问发起人。** 遇到未定义的细节,按以下优先级自行推断并继续:决策日志(§3)> 产品叙事(§2)> 功能规格(§6)> 工程常识。推断出的新决策追加到 §3 末尾,标注「执行者补充」。
2. **红线(§9)与人工确认节点(§6.0)在任何情况下不可移除、不可绕过、不可默认跳过。**
3. 本文档保持稳定,开发进度另记 `docs/progress.md`;只有决策日志允许追加。
4. 本项目预期由 AI 执行者**一次性完成全部 P0 与 P1**,不设阶段排期,交付以 §8 验收总标准为准;确需砍需求时按 P2 → P1 顺序砍,**P0 不可砍**。
5. 所有对 lark-cli、OpenCode CLI 的具体调用方式,以实测结果为准;实测与本文冲突时,以实测为准并记录到决策日志。飞书侧的命令名与参数**一律以 `lark-cli <domain> --help`、`lark-cli schema <s.r.m>`、`lark-cli skills read <skill>` 的实际输出为准**,不凭记忆或训练数据编写调用(接入总纲见 `LARK-CLI.md`)。
6. 主线链路「承诺 → 分身完成 → 私信迭代 → 发布 → 评审」是唯一的演示主线,任何实现取舍都**不得削弱它的稳定与流畅**;其余能力都是配角。

---

## 1. 一句话与背景

**Everyone 是装进飞书群里的「每个人的分身」:它带着你的全部上下文蹲在群里——帮你记、替你做、帮你审、帮你收、帮你答,人只负责拍板。**

黑客松硬约束(来自参赛指南):

- 作品运行时必须**同时**用到飞书和 AI,缺一个就不能完整工作;
- 发送、删除、发布类操作必须保留**人工确认**;
- 3 分钟以内**实机演示**视频,发社媒带话题 #飞书AI绝活大会;
- 提供**上手指南**,越开箱即用越好;
- 个人参赛,限投 1 个作品。

---

## 2. 产品叙事(核心世界观,发起人手稿的正式化)

> 本节是整个产品的「为什么」。所有功能取舍、优先级判断、文案口径都必须回到这一节。

### 2.1 飞书是什么地方

**飞书是人和人连接的地方。** 连接发生在两个场所:

- **群**:聊天记录可以被直接获取(机器人入群,`lark-cli event consume` 长连接监听);
- **会议**:会议本身不可直接获取,由用户把**会议记录 / 会议报告以文本形式粘贴进群**,Everyone 主动读取拆解。

### 2.2 连接的两个层面

人在飞书里的连接分两层:**工作**与**日常**。

### 2.3 工作层:两个对象,两个方向

工作层只有两类东西,方向相反:

| 对象 | 定义 | 方向 |
| --- | --- | --- |
| **任务** | 还没做的事,新出现的东西 | **外部 → 人**(外部把事压给人) |
| **报告** | 已经做完的产出 | **人 → 外部**(人把产出交给外部) |

### 2.4 Everyone 对「任务」(外部 → 人)的两种介入

1. **帮你记**:Agent 自动维护每个人的**四象限**(重要 × 紧急)和**排期表**。承诺(「我周五前交 X」)、@ 指派、会议记录里拆出的行动项,都会被记录并经本人确认后入账。
2. **替你做**:Agent 看到一个任务会**自我评估「这活我能不能干」**(报告 / 文档类为主,代码修改也支持)。能干,就在沙箱里替这个人完成 → **初稿私信本人** → 本人提意见快速迭代 → 本人拍板 → **由分身代表本人发回群里**(消息明确标注「Everyone 分身起草 · 本人已确认」,透明、不冒名)。

> 本质:AI 替人走完「外部输入 → 人 → 输出回外部」的闭环,人只保留判断权。

### 2.5 Everyone 对「报告」(人 → 外部)的两种介入

人自己做完的报告发进群后,Everyone 做两件事:

1. **分析**:从多个视角检查这份产出的质量;
2. **模拟外部反馈**:预设 2–3 个**带各自记忆的人设分身**(如数据严谨型、用户视角型、风控敏感型),以**回复的形式**逐条评论(**不做文档原文内嵌评论**——机器人做不到,这是明确的技术边界决策),且评论必须引用原文观点。

> 本质:AI 模拟外部环境,把外部世界的反馈**提前**返回给这个人。报告真正交付之前,已经过了一轮评审。

### 2.6 记忆让分身「替你在场」:帮你收、帮你答

分身的底气是记忆:群聊天记录、会议纪要、文档**全量**进入四层分层记忆(§6.F),任何地方可召回,召回必带出处。看得见一切之后,分身就能在你不在场时替你做两件事:

1. **帮你收**:群里出现意见 / 反馈 / 吐槽,分身判断「这归你管」,就替你记下来(写进你的记忆),并**私信**告诉你,附原文出处;你可以一键转成任务(进台账),或标记「不归我管」(分身据此修正判断)。
2. **帮你答**:群里有人提问,分身判断「这该你答,而且你的记忆答得了」——依据不限于 memory 与 skills,也包括你说过的聊天记录、你参加的会议纪要、你写过的文档——就以**你的分身**署名直接在群里代答,**答案必须带出处**;答错了你一键更正 / 撤回,不想要可在工作空间关掉。

> 本质:人不必时刻盯群。该你管的意见不会漏,你能答的问题不用等你上线。

### 2.7 日常层:闲聊 → 日报导读

群里的闲聊 Everyone 也跟踪。每天定时把全群聊天记录形成一份**日报(导读)**:群里发生了什么、有什么关键信息、有什么好玩的信息,让人快速回血,不错过也不淹没。

### 2.8 能力全景(一张表)

| 方向 | 对象 | Everyone 的介入 | 落点 |
| --- | --- | --- | --- |
| 外部 → 人 | 任务(未做) | 帮你记:四象限 + 排期 | 确认卡片 + 服务端渲染的四象限 / 排期图 + 多维表格 |
| 外部 → 人 | 任务(未做) | 替你做:沙箱完成 → 私信迭代 → 拍板发群 | 飞书文档 + 私信 + 群消息 |
| 人 → 外部 | 报告(已做) | 帮你审:多视角分析 | 分析回复 |
| 人 → 外部 | 报告(已做) | 模拟外部反馈:人设分身回复式评论 | 群内回复(引用原文) |
| 外部 → 人 | 意见 / 反馈 | 帮你收:判定责任人 → 记入其记忆 → 私信告知 | 私信卡片(知道了 / 转任务 / 不归我管) |
| 外部 → 人 → 外部 | 提问 | 帮你答:检索该人四层记忆,有把握则署名代答 | 群内回复(必带出处,可更正 / 撤回) |
| 日常 | 闲聊 | 日报导读 | 每日定时群消息 |

### 2.9 价值主张(评分「真实价值 30 分」的口径)

- 群里的承诺不再蒸发:说过的话被记住、被跟进、甚至被替你兑现;
- 交付前预评审:报告发出去之前已经过了一轮多视角反馈;
- 信息不过载:闲聊有导读,关键信息不漏;
- 人不在场也被代表:该你管的意见不漏收,你能答的问题不等你上线;
- **人只做两件事:拍板和迭代。**

### 2.10 对外强调的特性(材料、答辩、demo 的统一口径;只讲自己,不做对比)

1. **任务不是人发起的,是自动涌现的**:Everyone 没有「对话框里下指令」这个动作——任务从群聊承诺、@ 指派、会议记录里自己长出来,人第一次介入就是拍板。
2. **超强上下文**:群聊天记录、会议纪要、文档全部是分身的记忆,四层沉淀(原文 → 事实 → 场景 → 画像),任何地方可召回,每次召回带出处。
3. **单位是自然人**:一人一分身(记忆、技能、产物仓)。它替「你」记、做、审、收、答,不是一个群共用的问答机器人。
4. **拍板权永远在人**:对外输出要么事前确认,要么(帮你答)强制署名 + 出处、本人一键更正 / 撤回;所有代笔透明标注「分身起草 · 本人确认」。
5. **开箱即用、全栈可换**:开源、自部署,模型只要 OpenAI 兼容就能接,数据落在自己机器上;15 分钟装进任何一个群。

---

## 3. 决策日志(已拍板,不再讨论)

| # | 决策 | 内容 |
| --- | --- | --- |
| D1 | 产品名 | **Everyone**;口号「每个人的分身,替每个人做事」 |
| D2 | 执行引擎 | **OpenCode,headless(非交互)模式**,子进程调用 |
| D3 | 模型接入 | **OpenAI 标准格式 API**(`OPENAI_BASE_URL` + `OPENAI_API_KEY` + `MODEL` 三个环境变量,兼容任意 OpenAI-compatible 服务) |
| D4 | 任务域 | 工作报告 / 文档类为主,代码修改也支持 |
| D5 | 会议输入 | v1 仅支持**直接粘贴文本**进群;不做文件解析、不做妙记链接 |
| D6 | 记忆机制 | 手工预置 `memory.md` **+** 交互后自动沉淀(append,带日期) |
| D7 | 评论形式 | **回复式**(群内回复并引用原文),不做文档内嵌评论 |
| D8 | 架构 | 一台沙箱主机;**群级超级代理**统一调度;每人一个目录(`memory.md` + `skills/` + `repo/`),对外叙事为「每人一个分身」 |
| D9 | 评审团 | 2–3 个**固定预设人设**,不做「按群成员自动建模」 |
| D10 | Demo 数据 | 3–4 个**虚构成员**的测试群,全部假数据 |
| D11 | 管理后台 | 要做:**大屏**总览所有人数据 + **个人工作空间**(调记忆、调 Skill) |
| D12 | 人工确认 | 所有对外输出(发群、发文档、发评论、发日报以外的主动消息)必须经人确认;详见 §6.0 |
| D13 | 飞书租户 | 发起人自行准备并验证(见 §11 开放问题 O1) |
| D14 | 可视化口径 | 四象限 / 排期等一切可视化**由服务端渲染成图片**发送(SVG 模板填数据 → PNG → 飞书图片消息),不让模型用 Markdown 文本「画图」 |
| D15 | 飞书接入方式 | **一律走飞书官方 `lark-cli`(npm `@larksuite/cli`,MIT)子进程**,不引入 `@larksuiteoapi/node-sdk`:事件走 `lark-cli event consume <EventKey>` 读 NDJSON,写操作走对应 `+shortcut`,能力缺口用 `lark-cli api` 兜底。理由:免写 SDK 胶水与鉴权、免公网回调、命令即文档(`--help` / `schema` 自省),且**它本身就是「飞书 + AI」这条评分线的直接体现**。接入契约见 §7.5 与 `LARK-CLI.md` |
| D16 | 群消息可见性 | 目标态是机器人收全群消息,需敏感权限 `im:message.group_msg`(管理员审批);**未获批时按 §7.6 降级**,不阻塞主线 |
| D17 | 凭证管理 | 飞书凭证由 `lark-cli` 自己管(OS 钥匙串 + `~/.lark-cli`),**项目 `.env` 不再存 App Secret**;仓库只存群 ID、日报时间等非密配置 |
| D18 | 记忆系统 | 接入腾讯开源 **TencentDB Agent Memory**(MIT,TypeScript):L0 对话原文 → L1 原子事实 → L2 场景 → L3 画像四层分层记忆;配置 `embedding.provider="none"`,**免向量化**(BM25 / FTS5 全文检索),本地 SQLite;群聊、会议、文档全量入库,召回必带出处(§6.F) |
| D19 | 帮你收 | 群内意见 → 判定责任人 → 写入其记忆并私信本人(知道了 / 转任务 / 不归我管);默认开,个人可关(§6.J) |
| D20 | 帮你答 | 分身**署名直发**群内代答(发起人拍板):强制附出处、本人一键更正 / 撤回、个人工作空间可关;作为 §6.0 第二个订阅式例外(§6.J) |
| D21 | 意图识别两段式(执行者补充) | `intent_classify.md` 快模型粗分五类 → 按类型走细化 prompt(extract_commitment / extract_meeting / route_opinion / answer_check) |
| D22 | mock 前端(执行者补充) | 管理台内置「模拟群聊」开发工具页 + server 侧 MockAdapter,与真实模式共用同一业务代码;`CHAT_ADAPTER=mock\|lark` 切换;定位为开发工具,与交付产品分区 |
| D23 | 消息接收双通道(执行者补充) | live 同时启用 event consume(实时)与 user 身份轮询(5s),靠 message_id 幂等去重;应用「接收消息」权限未开通时轮询兜底,开通后自动秒级 |
| D24 | 文字指令兜底(执行者补充) | 「确认/忽略」精确匹配、「发布/放弃」前缀匹配的文字消息等价卡片按钮(人工确认铁律仍满足:本人亲口确认) |
| D25 | 记忆引擎落地形态(执行者补充) | Docker 容器 agentmemory/hermes-memory(standalone Gateway 8420)+ TS SDK;容器版为单记忆空间,隔离用 session_id 维度(person:<id> / group:<chatId>);L3 画像同步写 scenario 文件 |
| D26 | mock 产出文档(执行者补充) | mock 模式下产出以本地文档页(#/doc/<taskId>)替代飞书文档;live 模式走真实 docx |
| D27 | docs 域走 user 身份(执行者补充) | bot 缺 docx 应用权限,且「分身替本人创建的文档归本人名下」语义更优;全部 docs 调用 `--as user` |
| D28 | PRD-next(发起人 2026-08-28 口述) | 管理台升级工作台:分身接管视图、任务抽屉(runs 历史+沙箱文件透视)、四象限/排期手动调整;前端对标飞书设计语言重写;模拟群聊与产品分区。全部已实现(docs/prd-next.md) |
| D29 | 卡片更新走全员通道(执行者补充) | 卡片状态翻转一律 `PATCH /im/v1/messages/:id`(对全部成员生效,14 天窗口);延时更新 token+open_ids 只改点击者视图,仅作回落。实测:token 通道曾导致其他成员永远看到活按钮 |
| D30 | 卡片交互永不静默(执行者补充) | 任何点击必有可见结果:命中→原地更新(失败降级回复),未命中/失效/异常/非负责人→回复说明;全部卡片动作有文字指令等价物(确认/忽略/发布/放弃/知道了/转任务/不归我管/撤回/重试),卡片文案自带兜底提示 |
| D31 | 事件总线看门狗(执行者补充) | `event status --json` 的 `apps[].running` 为总线健康事实源;45s 探测,连续 2 次不健康→`event stop --all --force`+重建订阅。僵尸总线(进程活/连接死)为实测故障模式,卡片回调无轮询兜底必须靠自愈保实时 |
| D32 | 任务类型分化(执行者补充) | 能力自评输出 task_kind(doc/code/data/other);代码任务:OpenCode 提示词允许改 repo/、执行前后全仓哈希快照核对变更清单并附进报告、不做文本降级(诚实失败);repo 文件清单进自评依据 |
| D33 | 图片消息理解(执行者补充) | 图片双通道放行→`+messages-resources-download` 下载→视觉模型(实测网关 kimi-k3 支持 image_url)转写→描述文本进正常管道(意图/记忆/路由全生效);@机器人或私聊发图直接回看图结果;视觉不可用如实降级 |
| D34 | @机器人兜底对话(执行者补充) | @Everyone 未命中任何任务/意图 none/代答链路走不通时,以群近期聊天+本人记忆召回+任务清单为上下文正常回话;私聊固定帮助文案同样换成对话式。产品叙事「任务不是对话框下指令」不变——兜底对话不是指令入口,是不已读不回 |
| D35 | 虚拟成员治理(执行者补充) | live 模式虚拟成员(无飞书账号)不可私信→意见/提醒/初稿卡落后台(工作台可见)并记活动流;虚拟成员的署名代答任何真实成员可更正/撤回(否则永远无人能治理) |
| D36 | 入群快速指引(发起人 2026-08-28 需求①) | 新成员入群 → 私信「项目快速指引」卡(项目概况/进展/关键信息/成员分工/上手建议)。触发双通道:`im.chat.member.user.added_v1` 事件(控制台需订阅)+ 120s 群成员轮询兜底;同人 24h 幂等。上下文 = 群聊记录 + 任务台账 + 成员画像,LLM 生成失败降级模板拼接;信息不足时如实说明,禁止编造。虚拟成员指引落活动流。私信不属于 §6.0 需确认的对外输出 |
| D37 | 日报分层(发起人 2026-08-28 需求②) | 群里发总日报(原 FR-E 不变),同时逐人私信「今日分身战报」:替你做/替你答/替你收/今日新排/48h 将到期/已逾期。个人报为纯数据模板(零幻觉、不烧模型);当日无任何相关动作的人不打扰;虚拟成员记活动流不私信。定时与手动触发(/api/digest/run)都同时发两层 |
| D38 | 主动进度确认(发起人 2026-08-28 需求③) | tasks 新增 creator_id(发起者=源消息发送者)。未完成任务到期前 24h 与逾期各主动私信 owner 一次进度确认卡(已完成/顺利推进/有风险,文字指令等价);回应即转告发起者(creator≠owner 时);「有风险」后 30 分钟内的下一条私信作为原因转达发起者;发出 4h 无回应也通报发起者(每阶段一次)。「已完成」直接销账(status=published 同步 Bitable)。虚拟 owner 跳过私信只记活动流 |
| D39 | 排期同步飞书日程(发起人 2026-08-28 需求④,唯一允许降级项) | 任务确认入账(卡片/文字/修改后)= 用户接受 → 以 bot 身份在 Everyone 主日历建日程(截止前 1h 块,reminders 30min)并把 owner 加为参与人(open_id),日程落 owner 日历;改期 PATCH 同一日程,忽略/放弃/取消 DELETE;tasks 新增 calendar_event_id。缺 scope 时探测降级:活动流给 console_url 一键申请页,开通重启即转正。mock 模式同一代码路径,记模拟 event id。实测(2026-08-28):calendar:calendar 开通后 create/attendee/patch/delete 四通路全通;lark-cli api 的 query 一律走 --params JSON |
| D36 | 沙箱一任务一目录(2026-08-28 发起人指示) | 产物不再扁平存放:`tasks/<taskId>/{brief.md, draft.md, notes/}` 取代共享 `task-brief.md` 与 `outbox/<taskId>/draft.md`——共享任务书会被并发任务互相覆盖,是实测缺陷。读取兼容旧路径(历史任务仍可预览);前端沙箱页做访达式目录树+面包屑+预览 |
| D37 | 前端按角色分视图(2026-08-28 发起人指示) | 登录页两种身份(选人 = 团队成员 / 管理员按钮),演示环境不鉴权。成员只见自己的任务与分身,全员排期只读可见(知道谁在忙,看不到细节);全局实时动态、全员台账与批量操作、评审与日报仅管理员。越权路由双向纠正(连 URL 一起)。接真实飞书后管理员判定应改为飞书角色/群管理员 |
| D40 | 预制 skill 库(2026-08-28 发起人指示) | `templates/preset-skills/` 随产品发布 6 个跨角色通用模板(会议纪要/周报/方案选型/进展同步/调研综述/操作手册),`GET /api/skills/presets` 解析标题与一句话简介。技能页「从预制库添加」可直接装或先改再装;装上 = 复制进本人沙箱 `skills/`,此后归本人所有、随便改,预制库不再跟踪。同名标「已装过」,覆盖需二次确认。新人的分身不必从空白起步 |
| D41 | 任务书按相关度选注入 skill(2026-08-28) | 技能超过 3 个时只注入与任务标题最相关的 3 份全文,其余压成一行目录(OpenCode 可自行读 `skills/<名>.md`)。中文无空格,相关度用 2-gram 交集排序——只用于排序,排错的代价仅是少一份模板。技能库很小(总字数 <3500)时不做取舍。动机:一份 skill 约 1500 字,四份以上会盖过任务描述与记忆要点,且给「写竞品分析」塞「版本发布说明」纯属干扰 |

---

## 4. 角色

| 角色 | 说明 |
| --- | --- |
| 群成员 | 被服务者。承诺、被 @、发报告、看日报、私信里拍板 |
| 管理员(装机人) | 把 Everyone 装进群、配置成员与人设、看大屏 |
| 超级代理 | 群级唯一的调度中枢:监听、识别、记账、派活、路由意见、调度代答、发日报 |
| 个人分身 | 超级代理按需唤起的「某个人的执行态」:加载该人的 memory + skills + repo 后干活;群里代答时的署名主体 |
| 评审人设分身 | 预设人设(内置三个,名字与口味见 §7.7),只做回复式评论 |

---

## 5. 核心场景(验收级走查)

### S1 承诺被记住(P0)
小明在群里说「周五前我把竞品报告发出来」→ 超级代理识别出承诺(owner=小明,due=周五)→ 群内发确认卡片(确认 / 修改 / 忽略)→ 小明点确认 → 写入多维表格 → 机器人在源消息下回复:一张**服务端渲染**的小明最新四象限图(新任务落点高亮)+ 台账链接。
**成功标准**:从发言到卡片 ≤ 5 秒;确认后表格记录字段完整,群里收到更新后的四象限图。

### S2 会议记录拆解(P0)
有人把会议记录文本粘贴进群(约定触发:消息以 `#会议` 开头,或 @ 机器人并附长文本)→ 拆出结论、行动项(owner / due)→ 逐条卡片确认 → 入台账。
**成功标准**:一段 500 字会议记录拆出 ≥ 3 条行动项,owner 识别正确。

### S3 替你做(P0,demo 核心)
任务入账时分身自评「能不能干」;能干 → 到期前(或被 @ 时)超级代理唤起小明的个人分身(加载 `workspaces/xiaoming/` 的 memory + skills)→ OpenCode headless 在该目录跑任务 → 产出写成**飞书文档** → **私信小明**卡片(查看 / 提意见 / 发布)→ 小明文字提意见 → 迭代新版本 → 小明点**发布** → 以摘要 + 文档链接形式发群。
**成功标准**:一句承诺最终变成一篇发进群的文档,全程小明只打了一次修改意见、点了一次发布。

### S4 模拟评审(P1)
报告(S3 产出或人自己发的文档链接)发群后 → 三个人设分身依次以回复形式评论,每人 ≤ 3 条,每条必须引用原文观点 → 作者采纳 / 回复。
**成功标准**:发群后 60 秒内出现 3 条角度明显不同、引用了原文的回复。

### S5 日报导读(P1)
每天 18:30(可配)→ 汇总当日群消息 → 生成导读:关键决策 / 新增承诺与状态 / 被 @ 未回应 / 明日到期 / 有趣瞬间 1–2 条 → 发群。
**成功标准**:200+ 条消息的群,导读 ≤ 300 字且关键信息无遗漏。

### S6 管理后台(P1)
管理员打开大屏:全群任务四象限分布、排期甘特、承诺完成率、Agent 实时活动流。个人选择自己的身份进入工作空间:在线编辑 `memory.md`、启停 / 编辑 skills、浏览自己的 repo 与产出历史。
**成功标准**:S1–S5 产生的数据在大屏实时可见;改完 memory 后下一次执行生效。

### S7 记忆沉淀(P1)
S3 每轮迭代结束后,把「小明的偏好 / 修改意见要点」提炼 1–3 条,append 进 `workspaces/xiaoming/memory.md`(带日期与来源),去重。
**成功标准**:第二次替小明写报告时,不再犯第一次被指出的问题。

### S8 帮你收(P1)
群里有人说「登录后的空白页真的很影响体验」→ 超级代理结合记忆判定这属于小明负责 → 写入小明记忆(类型=意见)→ 私信小明:引用原文 + 出处链接 + 按钮(知道了 / 转为任务 / 不归我管)→ 点「转为任务」则走 S1 确认入账。
**成功标准**:私信 ≤ 30 秒送达且带出处;「转为任务」一键入台账;点过「不归我管」后,同类意见不再误派给小明。

### S9 帮你答(P1)
新人在群里问「上次说的活动预算口径是多少?」→ 超级代理判定该小明答 → 在小明的四层记忆中命中 8/20 会议纪要相关段落 → 群内以「小明的分身」署名回答,附「出处:8/20 会议纪要」→ 小明事后可一键更正 / 撤回。
**成功标准**:代答必须带出处;检索不到依据就不发群(改为私信小明提醒并附建议答案);更正 / 撤回一步完成。

---

## 6. 功能规格

### 6.0 人工确认铁律(P0,贯穿所有模块)

以下动作**必须**由对应的人在卡片上显式确认后才执行,无确认则任务停在待确认态:

1. 任务入台账(承诺 / 会议行动项 → 本人确认);
2. 分身产出发布到群(owner 点「发布」);
3. 任何以他人名义或面向全群的主动消息。**仅有两个订阅式例外**:日报(管理员可开关)与帮你答代答(D20:强制署名 + 出处,本人一键更正 / 撤回,个人可关)。例外只免「事前确认」,不免透明署名与可撤回。

### 6.A 消息接入与理解(P0)

- FR-A1 通过 `lark-cli event consume im.message.receive_v1 --as bot` 子进程接收群消息(CLI 内部维持长连接,**免公网回调**);事件以 **NDJSON 逐行**输出到 stdout,server 按行解析入队。**一个 EventKey 一个进程**,卡片回调另起一个 `card.action.trigger` 进程(§7.5);
- FR-A2 消息幂等(message_id 去重);CLI 明确建议以 `message_id` 而非 `event_id` 作幂等键(重复推送场景);
- FR-A2b 事件字段口径以 `lark-cli event schema <key>` 为准,**不套用 OAPI 原始报文结构**:`im.message.receive_v1` 已被 CLI 拍平(字段在顶层 `.xxx`),`.content` 对 text/post/image 等已渲染成纯文本(@ 已解析为显示名),**仅 `interactive` 卡片才是 JSON 字符串**(需 `fromjson`);`sender_id` 只有 open_id,姓名需另查通讯录;
- FR-A3 LLM + 规则识别五类意图:承诺(第一人称+时间+交付物)、@ 指派、`#会议` 文本、**意见 / 反馈**(指向某个负责域的评价或吐槽)、**提问**(期待某人回答的疑问);低置信度(<0.6)只入「待确认列表」(后台可见),不发卡片打扰;
- FR-A4 识别结果带出处(源消息链接);
- FR-A5 全量入记忆:所有群消息(含闲聊)、会议文本、产出与外发文档统一 capture 进分层记忆 L0(§6.F),按人归属,供全局召回。

### 6.B 任务台账与四象限 / 排期展示(P0)

- FR-B1 多维表格(Bitable)为对外台账,字段:任务名 / 负责人 / 来源(承诺|会议|@指派|手动)/ 重要 / 紧急 / 象限 / 截止时间 / 状态(待确认|待办|分身进行中|待审阅|已发布|已取消)/ 产出链接 / 源消息链接 / 创建时间;
- FR-B2 Bitable 视图:四象限(看板按象限分组)+ 排期(甘特按截止时间),作为「可操作」的台账;
- FR-B3 确认卡片:确认 / 修改(改 owner、due)/ 忽略;重要 × 紧急由 LLM 初判,卡片上可改;
- FR-B4 内部状态镜像存 SQLite(§7.3),Bitable 写失败可重试;
- FR-B5 **四象限图(服务端渲染,D14)**:2×2 布局,横轴紧急、纵轴重要;每格列出该人未完成任务(名称 + 截止日,最多 6 条,溢出显示「+N」);本次变更的任务高亮描边;底部一行统计(待办 / 进行中 / 本周到期)。实现:SVG 模板填数据 → 转 PNG → `lark-cli im +messages-send --image <png>`(**CLI 接受本地路径并自动完成上传 + 发送,不需要单独调上传接口**)。工程约束:该参数只收 **cwd 相对路径**,绝对路径与 `..` 会被拒,故渲染产物必须落在 CLI 子进程 cwd 之下(§7.5);
- FR-B6 **排期图(服务端渲染,D14)**:横向 7 天时间轴甘特,行 = 任务,色块 = 状态;渲染管线同 FR-B5;
- FR-B7 展示时机:① 任务确认 / 状态变化后,在源消息下回复 owner 的四象限图;② 群内 @Everyone 说「四象限」或「排期」(可带 @某人)→ 回对应图;③ 日报携带全群排期图(6.E);④ 管理后台大屏为实时版(6.G)。

### 6.C 分身执行(P0)

- FR-C1 触发:任务确认入账时能力自评(prompts/capability_check.md,输出 JSON `{can_do, reason, plan}`:文档报告类 → 能干;需要外部私有信息且记忆里没有 → 不干,并在台账标注原因);到期前 24h 兜底触发;群内 @Everyone+任务 手动触发;
- FR-C2 工作区:`workspaces/<person>/`,含 `memory.md`、`skills/`(markdown 指令文件,每个 skill 一个文件)、`repo/`(该人的私有产物仓)、`tasks/<task-id>/`(一任务一目录,D36);
- FR-C3 执行:在任务目录生成 `tasks/<task-id>/brief.md`(五段固定结构:任务描述 / 源消息与群上下文摘录 / 记忆要点(memory.md + 四层召回 Top-K,带出处,§6.F)/ 可用 skills 清单 / 产出要求与文件路径)→ 子进程调 `opencode run`(headless,cwd=该人工作区,模型走 D3 配置)→ 产出 markdown 落 `tasks/<task-id>/draft.md`,过程笔记落同目录 `notes/`;
- FR-C4 产出转飞书文档:`lark-cli docs +create --doc-format markdown --content @tasks/<task-id>/draft.md --title "<标题>"`(多行内容**必须**用 `@file` 或 stdin 传入,避免 shell 转义损坏);
- FR-C5 私信迭代:私信卡片三个按钮 = 查看初稿(文档链接)/ 发布 / 放弃,并提示「直接回复文字即修改意见」;收到文字意见 → 意见追加进任务书(brief.md) → 重跑 → **更新同一篇飞书文档**(不另建,`lark-cli docs +update --doc <token> --command overwrite --doc-format markdown --content @<新稿>`)→ 再发一条迭代卡片;迭代次数无上限;
- FR-C6 发布:owner 点发布 → 群内发「@owner 的报告已完成(Everyone 分身起草 · 本人已确认)」+ 摘要 + 文档链接,台账状态 → 已发布;
- FR-C7 失败处理:执行报错 → 私信 owner 告知并可一键重试;超时(>10min)同上。

### 6.D 报告反馈:分析 + 评审团(P1)

- FR-D1 人设文件 `personas/<name>/persona.md`(口味、关注点、语气)+ `memory.md`(该人设的立场记忆);
- FR-D2 触发:S3 发布事件,或群内消息包含飞书文档链接且 @Everyone 求评审;文档内容经 `lark-cli docs +fetch --doc <url> --doc-format markdown` 读取(分身自建的文档天然有权限;外部文档无权限时回复提示「授权机器人或直接粘贴全文」);
- FR-D3 先发一条**结构化分析回复**:四个维度(数据可靠 / 逻辑完整 / 可读性 / 风险)各给 1–5 分 + 一句话依据;
- FR-D4 随后每个人设 ≤ 3 条评论,以「回复该消息」形式发群,格式固定:「> 引用原文句子」+ 评论正文 + 人设署名;
- FR-D5 分析与评论受管理员开关控制(默认开,demo 群开)。

### 6.E 日报导读(P1)

- FR-E1 每日定时(默认 18:30,可配)拉取当日群消息 → LLM 生成导读 → 以飞书卡片发群,固定五段:关键决策 / 新增承诺与状态 / 被 @ 未回应 / 明日到期 / 有趣瞬间(1–2 条),全文 ≤ 300 字;
- FR-E2 卡片附当日**全群排期图**(FR-B6 渲染)与承诺完成率一行;
- FR-E3 管理员可开关、可改时间、可手动立即生成(后台按钮)。

### 6.F 记忆系统:四层分层、全量上下文(P0 接入 / P1 全量能力)

- FR-F1 引擎:**TencentDB Agent Memory**(D18)以 Gateway 常驻子进程接入(默认 `127.0.0.1:8420`,TS SDK / HTTP);四层 = L0 对话原文 / L1 原子事实 / L2 场景块 / L3 画像;`embedding.provider="none"`,免向量化,BM25 / FTS5 检索,SQLite 后端,除 LLM API 外零外部依赖;
- FR-F2 入量:FR-A5 的全部内容 capture 进 L0,L1–L3 由引擎管道自动提取;隔离维度:team = 群,user = 成员;
- FR-F3 召回:凡需要「这个人知道什么」的场合(能力自评、task-brief 组装、帮你收责任判定、帮你答检索、评审引用)统一走 recall API,**召回结果必须带出处**(消息链接 / 会议日期 / 文档链接);
- FR-F4 memory.md 定位:`workspaces/<person>/memory.md` 保留为 L3 画像的**人工可编辑视图**——手工预置与 S7 沉淀写这里,启动与保存时同步进引擎;后台编辑即生效(6.G);
- FR-F5 执行注入:task-brief 记忆段 = memory.md 全文(≤2k tokens,超长取最近 + 置顶段)+ recall Top-K 相关记忆(带出处);
- FR-F6 降级:引擎不可用 → 退化为 memory.md 直读 + SQLite LIKE 检索,帮你答自动停用(出处无保障),主线 S1–S3 不受影响(见 6.I)。

### 6.G 管理后台(P1)

- FR-G1 全局视图(管理员):四象限分布图、排期甘特(行内可改截止日)、承诺完成率(确认数 / 完成数)、Agent 活动流(时间线:识别了什么、跑了什么、发了什么);
- FR-G2 个人工作台(成员):demo 级登录(登录页选人即入,D37);需我拍板 / 我的任务 / 我的四象限 / 分身正在做 / 全员排期(只读);「我的分身」= memory.md 编辑器(保存即生效)、skills 列表(启停 / 编辑 / 新建)、任务与产出、代收代答、沙箱文件(访达式目录浏览,D36);
- FR-G3 数据来源:SQLite + 文件系统,轮询或 SSE 刷新,不追求生产级;
- FR-G4 视图分权(D37):成员导航只有「工作区 + 我的分身」,看不到团队页、全局实时动态、别人的任务明细、评审与日报开关;越权路由回各自首页。

### 6.H 上手与复用(P0,评分「可复用 20 分」)

- FR-H1 `.env.example`:OpenAI 兼容 API 三件套、群 ID、日报时间。**不含飞书 App Secret**——飞书凭证由 lark-cli 自管(D17),`.env` 只留 `LARK_CLI_BIN`(默认 `lark-cli`)这类非密项;
- FR-H2 一键启动:`npx @larksuite/cli@latest install` → `lark-cli config init --new` → `lark-cli auth login --recommend` → `pnpm i && pnpm dev`;`pnpm dev` 启动时先跑 `lark-cli doctor` 与 §7.4 的 `auth check` 断言,缺什么直接打印补授权命令。装机全流程写进 `LARK-CLI.md`,README 引用它;
- FR-H3 Bitable 模板(可复制链接)、人设模板、skill 模板各一份;
- FR-H4 README = 15 分钟上手指南(装进自己的群为验收标准);
- FR-H5 自测脚本:`pnpm smoke` 不连飞书跑通「模拟消息 → 识别 → 台账 → 四象限渲染 → task-brief 生成」全链路;`pnpm smoke:live` 读 .env 后发一条真实私信 + 一张渲染图。交付前两者必须通过。

### 6.I 演示模式与降级矩阵(P0)

- FR-I1 `DEMO_MODE`:与真实模式走同一套代码路径,允许预热任务与缓存产物「真实重放」,保证 3 分钟视频与现场演示不冷场;不伪造界面与结果,只缩短等待;
- FR-I2 降级开关(装机遇阻时逐级退化,不整体崩溃):

| 故障点 | 降级方案 |
| --- | --- |
| OpenCode 不可用 | 内置 executor:直接按 task-brief 调 OpenAI 兼容 API 生成 |
| docx 无权限 | 产出改为 markdown 长消息发送 |
| Bitable 受限 | 仅 SQLite;四象限 / 排期图与管理后台照常(均读 SQLite) |
| **`im:message.group_msg` 未获批** | 显式触发 + 用户身份轮询补齐(§7.6),主线不变 |
| **`event consume` 进程异常退出** | 指数退避重启 + 重启后用 `im +chat-messages-list` 回捞断连期间消息(靠 `message_id` 幂等去重),不丢事件 |
| **某个 `+shortcut` 不好用或缺参数** | 同一份参数改走 `lark-cli api <METHOD> <path>` 原生端点(2500+ API 全覆盖),封装层接口不变 |
| **记忆引擎不可用** | memory.md 直读 + SQLite LIKE 检索;帮你收 / 帮你答自动停用,主线 S1–S3 照常 |

### 6.J 帮你收与帮你答(P1)

- FR-J1 帮你收判定:FR-A3 识别出「意见 / 反馈」→ 责任人判定(`prompts/route_opinion.md`,输入 = 意见内容 + 各成员 L3 画像摘要 + 任务台账归属),置信度 <0.6 只进后台待认领列表,不打扰任何人;
- FR-J2 帮你收动作:写入责任人记忆(L1,类型 = 意见)→ 私信卡片:引用原文 + 出处链接 + 三按钮(知道了 / 转为任务 / 不归我管);「转为任务」复用 6.B 确认入账;「不归我管」记入记忆,用于修正后续判定;
- FR-J3 帮你答判定:FR-A3 识别出「提问」→ 判定应答人 → 对其四层记忆全域检索(memory.md、skills、L0 聊天原文、会议纪要、产出文档),`prompts/answer_check.md` 评估把握;
- FR-J4 帮你答门槛:检索结果能直接支撑答案(存在可引用出处)才代答;否则**不发群**,改为私信该人「群里有个问题可能该你答」并附建议答案;
- FR-J5 帮你答格式:群内回复「(某某的分身)答:…」+「出处:…」(消息链接 / 会议纪要日期 / 文档链接);消息附「更正 / 撤回」按钮(仅本人可点),撤回即删除该消息并把负反馈记入记忆;
- FR-J6 开关与审计:两能力在个人工作空间独立开关(默认开),管理员可全群关;全部动作写入 `assists` 表(§7.3)并进大屏活动流。

---

## 7. 技术方案

### 7.1 架构

```
飞书开放平台
  ▲ 写:lark-cli im/docs/base +shortcut(一次调用一个子进程)
  │ 读:lark-cli event consume(常驻子进程,NDJSON → stdout)
  │      ├─ im.message.receive_v1   进程 A
  │      └─ card.action.trigger     进程 B
  ▼
apps/server(Node 20 + TS,超级代理)
  ├─ lark:唯一的 lark-cli 封装层(§7.5)——常驻 consumer 管理 + 一次性命令执行 + JSON 信封解析
  ├─ ingest:消息解析、幂等、意图识别(LLM)
  ├─ memory:四层记忆(TencentDB Agent Memory Gateway 常驻,BM25 免向量,capture / recall)
  ├─ ledger:任务台账(SQLite ←同步→ Bitable)
  ├─ executor:工作区管理 + OpenCode headless 子进程
  ├─ reviewer:分析 + 人设评审
  ├─ presence:帮你收(意见路由私信)+ 帮你答(署名代答)
  ├─ digest:日报定时器
  ├─ render:图片渲染(SVG 模板填数据 → PNG,交给 lark 层发送,D14)
  └─ admin-api:REST/SSE → apps/admin(Vite + React;产品前端 index.html + 模拟群聊 simulator.html)

workspaces/<person>/{memory.md, skills/, repo/, tasks/<task-id>/{brief.md, draft.md, notes/}}
personas/<name>/{persona.md, memory.md}
prompts/*.md(所有提示词文件化)
templates/*.svg(四象限图 / 排期图模板)
记忆数据:TencentDB Agent Memory 自管(本地 SQLite,默认 ~/.memory-tencentdb)
```

### 7.2 技术栈(执行者按此实施,未列明的小库自选并记录)

- TypeScript + Node 20,pnpm monorepo:`apps/server`、`apps/admin`、`packages/shared`;
- 飞书:**`lark-cli`(npm `@larksuite/cli`)子进程**,读走 `event consume` 常驻长连接、写走 `+shortcut`、缺口走 `lark-cli api`;**不引入飞书 Node SDK**(D15)。注意 CLI 自带的 npm 包装脚本要求 **Node ≥ 20.12**(用到 `node:util` 的 `styleText`);
- LLM:OpenAI 兼容 REST(官方 `openai` npm 包,base URL 可配);
- 执行引擎:OpenCode CLI headless(`opencode run "<prompt>"`,cwd 指向个人工作区;**实测 1.18.13 可用**)。自定义模型网关走仓库根的 `opencode.json`,provider 用 `@ai-sdk/openai-compatible`,`baseURL` / `apiKey` 一律写成 `{env:...}` 占位由 `.env` 注入,**密钥不落配置文件**;
- 记忆:**TencentDB Agent Memory**(npm `@tencentdb-agent-memory/*`,D18)Gateway 常驻子进程 + TS SDK;`embedding.provider="none"`(免向量化、免 embedding 服务),SQLite 后端;capture / recall 契约见 §6.F;
- 图片渲染:SVG 模板 + `@resvg/resvg-js` 转 PNG(等效轻量方案可替换;不引入无头浏览器);
- 存储:SQLite(`better-sqlite3`)+ 飞书多维表格(对外台账)+ 文件系统(记忆/技能/产出);
- 后台:Vite + React,图表用轻量库(如 recharts)。

### 7.3 数据模型(SQLite 核心表)

- `persons(id, feishu_open_id, name, workspace_dir)`
- `tasks(id, bitable_record_id, owner_id, title, source, due_at, status, confidence, src_msg_link)`
- `runs(id, task_id, engine, status, started_at, finished_at, doc_token, iteration)`
- `messages_seen(msg_id, processed_at)`
- `memory_events(id, person_id, content, source_run_id, created_at)`
- `assists(id, type: collect|answer, person_id, src_msg_link, evidence_link, content, status, created_at)`(帮你收 / 帮你答审计)
- `review_comments(id, run_id, persona, quoted_text, content, created_at)`

### 7.4 飞书权限与开通清单(O1 逐项验证)

装机不再是「在开放平台后台手工建应用 + 勾权限」,而是三条命令:`lark-cli config init --new`(浏览器里建应用)→ `lark-cli auth login --recommend`(授权)→ `lark-cli doctor`(自检)。`--recommend` 覆盖的是**免管理员审批**的常用权限,本项目除下表两项外均在其中。

| # | 能力 | 关键 scope / 开关 | 获取方式 |
| --- | --- | --- | --- |
| 1 | **收群内全部消息(无需 @)** | `im:message.group_msg`(**敏感,需管理员审批**);只要用户消息不要机器人消息用它,要含机器人消息用 `im:message.group_msg.include_bot:read` | 开放平台后台申请 + 管理员批准;未获批走 §7.6 降级 |
| 2 | 只收 @ 机器人的消息(保底) | `im:message.group_at_msg:readonly` | 免审批 |
| 3 | 收私信 | `im:message.p2p_msg:readonly` | 免审批 |
| 4 | 卡片回调 | `im:message:readonly` + 后台事件开关 **`card.action.trigger`** | 免审批,但**必须在开发者后台手工开启该事件** |
| 5 | 发群消息 / 私信 / 图片 | `im:message`、`im:resource` | `--recommend` 已含 |
| 6 | 多维表格读写 | `base:app:*` / `base:table:*` / `base:record:*` / `base:view:*` | `--recommend` 已含 |
| 7 | 云文档创建 / 编辑 | `docx:document:create`、`docx:document:write_only`、`docs:document.content:read` | `--recommend` 已含 |
| 8 | open_id ↔ 姓名 | `contact:user:search`、`im:chat.members:read` | `--recommend` 已含 |
| 9 | 机器人本体 | 应用启用**机器人能力**并被拉进目标群 | 后台开启 + 群内添加 |

补授权用 `lark-cli auth login --scope "<scope>" --no-wait --json` 拿链接,授权后 `lark-cli auth login --device-code <code>` 收尾;随时用 `lark-cli auth check <scope>`(exit 0 = 有)在启动自检里断言。

> 实测记录(2026-08-27,发起人租户):`config init` / `auth login --recommend` / `doctor` 全通过,bot 与 user 双身份 ready;`--recommend` **不含**日历与上表第 1 项。

### 7.5 lark-cli 接入契约(执行者必须遵守)

完整版见根目录 `LARK-CLI.md`,以下是**不可违反**的部分:

- **收敛**:所有 `lark-cli` 调用只允许出现在 `apps/server/src/lark/` 一层,业务模块调该层的类型化函数,禁止在业务代码里散落 `spawn('lark-cli')`;
- **常驻 consumer 的子进程契约**:
  - 启动后阻塞读 **stderr**,直到出现 `[event] ready event_key=<key>` 再开始读 stdout,**不许用 `sleep` 兜底**;
  - **不要关掉 stdin**:无界运行时 stdin EOF 会被当成优雅退出信号(Node 里 `spawn` 的 `stdio` 需保留可写 stdin,不能设 `'ignore'`);
  - 退出用 **SIGTERM**,**禁止 `kill -9`**(会漏掉服务端退订,导致重启报「订阅已存在」或重复投递);
  - 退出码语义:`0` = limit/timeout/signal(正常),`1` 业务失败、`2` 参数非法、`3` 鉴权失败(含 `missing_scope`)、`4/5` 网络或内部错误;server 对非 0 退出做**指数退避重启**并在大屏活动流告警;
- **错误处理按信封,不按文案**:成功信封在 stdout 且 `ok:true`;失败信封在 stderr,形如 `{"ok":false,"error":{"type","subtype","code","hint",...}}`。判断成败一律看 **`ok` 字段或进程退出码**,**不要看 `code == 0`**(成功信封根本没有 `code`);缺权限时读 `error.missing_scopes` 自动生成补授权提示,**不要正则匹配 message 文本**;
- **写操作**:所有发送类命令带 `--idempotency-key`(用 task-id / message-id 派生);高危命令需 `--yes`,而 §6.0 要求的人工确认在**卡片层**已经拦过一次,代码里不得为了省事跳过;
- **身份**:面向群/成员的一切输出显式带 `--as bot`,读取个人数据才用 `--as user`,不依赖 `auto`;
- **路径**:`--image` / `--file` 只接受 cwd 相对路径,渲染产物与附件必须落在子进程 cwd 之下。

### 7.6 未获批敏感权限时的降级(承接 D16)

若第 1 项 `im:message.group_msg` 未获批,主线不改结构,只换消息来源:

| 层 | 目标态 | 降级态 |
| --- | --- | --- |
| 触发 | 机器人实时收全群消息 | `@Everyone` / `#会议` 显式触发(`im:message.group_at_msg:readonly`)+ 每 N 分钟以**用户身份**轮询补齐(`lark-cli im +chat-messages-list --chat-id <oc_> --as user`,scope `im:message.group_msg:get_as_user`,`--recommend` 已含,**已实测可用**) |
| 时延 | 秒级 | 显式触发秒级;被动承诺识别延迟 ≤ 轮询周期 |
| 影响面 | 全量 | S1(承诺)与 S5(日报)从「实时」变「准实时」,其余场景无损 |

Demo 剧本(§10)对时延敏感的镜头一律走**显式触发**路径,因此降级态也能完整演示。

### 7.7 提示词与人设资产(全部文件化,评分「可复用」的一部分)

`prompts/`:`extract_commitment.md`、`extract_meeting.md`、`capability_check.md`、`task_brief.md`、`memory_distill.md`、`daily_digest.md`、`route_opinion.md`(意见责任人判定)、`answer_check.md`(代答把握评估);
`personas/`:内置三人设——**老 K**(数据严谨,盯口径与来源)、**小鹿**(用户视角,问「这对读者有什么用」)、**谨叔**(风控敏感,挑风险与合规漏洞)。

---

## 8. 验收总标准(Definition of Done,不设阶段排期)

一次性交付,完成即验收。全部满足才算完成:

1. 场景 S1–S9 逐条通过(以 §5 各自的成功标准为准);
2. 地基自检:`pnpm smoke` 与 `pnpm smoke:live`(FR-H5)双通过;`lark-cli doctor` 全绿,`event consume` 常驻进程能收到群消息并触发一条私信,SIGTERM 后进程以退出码 0 干净退出;`opencode run` 能在个人工作区产出文件;四象限图能渲染成 PNG 并发进群;记忆引擎能 capture 群消息并 recall 命中(带出处);
3. Demo 剧本(§10)在干净环境从头到尾跑通;
4. 仓库无任何密钥与真实数据;所有对外发送动作有人工确认;
5. README 按 §6.H 可独立复现(15 分钟装进一个新群)。

---

## 9. 红线(不可越)

1. 真实同事的聊天记录、姓名、公司内部数据**禁止**出现在代码、demo、视频、截图、表单、公开仓库中——demo 全部用虚构人物与假数据复刻;
2. Token / 密钥只走环境变量,`.env` 进 `.gitignore`,开源前全仓扫描;
3. 第三方依赖(OpenCode、飞书 SDK、模型服务)在 README 注明来源与协议;
4. 不刷票、不伪造效果;演示为实机运行;
5. 人工确认节点(§6.0)不可移除。

---

## 10. Demo 剧本(3 分钟)与提交物

| 时间 | 画面 | 观众记住什么 |
| --- | --- | --- |
| 0:00–0:20 | 群聊快闪:@ 了没回音、「我明天发你」然后消失 | 群里的承诺会蒸发 |
| 0:20–1:00 | 「周五前我出竞品报告」→ 卡片确认 → 群里回一张实时渲染的四象限图 | 说过的话被记住了 |
| 1:00–1:50 | 分身开工 → 私信初稿 → 提意见 → 秒级迭代 → 点发布进群 | 承诺被替你兑现 |
| 1:50–2:30 | 初稿里预埋 3 处缺陷,老 K / 小鹿 / 谨叔各精准命中一处 → 作者只拍板;切一眼管理后台大屏 | 评审也不用等人 |
| 2:30–3:00 | 日报导读 + 一周承诺完成率 before/after + 开源仓库 | 你的群也能装一个 |

若节奏允许,在 1:50 前插 10 秒「帮你答」镜头:新人提问 → 被问到的人不在线 → 他的分身带出处代答——「分身替你在场」最直观的一幕,也是超强上下文的证明。

提交物:表单(产品介绍 + 上手指南 + Demo 链接)、≤3 分钟实机视频(社媒带 #飞书AI绝活大会)、开源仓库、Bitable 模板链接。表单文案由发起人**手打 / 口述**,不粘贴 AI 生成内容(赛事要求)。

**证据策略(真实价值 30 分)**:发起人从开发首日起用 Everyone 管理**本项目自身的开发承诺**(真实使用、个人数据、不涉公司信息);材料中的 before / after 与台账截图均来自这份真实记录,Demo 群的虚构成员仅用于视频叙事。

**投票期策略**:视频发布后,投票期内补发一条「它替我管着做它自己的任务」制作过程贴拉票;所有社媒文案由发起人手写。

---

## 11. 开放问题(仅剩这些)

| # | 问题 | 负责人 |
| --- | --- | --- |
| O1 | ~~飞书租户与权限验证~~ **已部分关闭**:lark-cli 装机、双身份授权、doctor 自检、群列举与群消息读取均已实测通过(§7.4 实测记录)。**剩余**:① `im:message.group_msg` 敏感权限能否获批;② 后台开启 `card.action.trigger` 事件;③ 机器人拉进 demo 群 | 发起人 |
| O2 | ~~`opencode run` 参数、模型配置、退出行为实测~~ **已关闭**(2026-08-27,OpenCode 1.18.13):`opencode run "<prompt>"` headless 可用,退出码 0;通过 `opencode.json` 的 `openai-compatible` provider 接自建网关成功;**在指定 cwd 下能自主创建目录与文件、无需交互确认**,即 FR-C3「产出落任务目录」成立。剩余待观察:长任务超时行为(`OPENCODE_TIMEOUT_SEC`)与失败退出码分布 | 执行者 |
| O3 | Demo 群虚构成员账号来源(个人版飞书小号) | 发起人 |

---

## 附录 · 术语表

| 术语 | 含义 |
| --- | --- |
| 超级代理 | 群级唯一调度中枢,监听群、记账、派活、发日报 |
| 个人分身 | 超级代理加载某人的 memory/skills/repo 后的执行态 |
| 评审人设 | 预设的评论角色(老 K / 小鹿 / 谨叔),模拟外部反馈 |
| 台账 | 多维表格里的任务总表(四象限 + 排期两个视图) |
| 导读 | 每日群聊摘要 |
| 帮你收 | 群内意见 → 判定责任人 → 记入其记忆并私信本人 |
| 帮你答 | 分身检索本人四层记忆,有把握时署名代答(必带出处,可更正 / 撤回) |
| 四层记忆 | TencentDB Agent Memory:L0 原文 / L1 事实 / L2 场景 / L3 画像,免向量化 |
| 目标模式 | AI 执行者按本 PRD 自主开发、不逐步请示的工作方式 |
