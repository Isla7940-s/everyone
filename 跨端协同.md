# Everyone 本地 Agent 工作记录与远程协作需求

## 一、项目目标

构建一套“本地 Agent 工作记录 + 云端 Agent 远程协作”的系统。

用户可能使用 Codex、Cursor、Claude Code 等不同的本地 AI 工具。用户授权后，本地 Agent 可以读取这些工具中的工作会话，并将工作总结、任务完成情况和时间去向同步到 Everyone 云端。

云端 Everyone 中运行的 Agent 可以查询这些工作总结。当云端 Agent 缺少完整上下文时，可以请求用户本地的 AI Agent 协助完成工作。用户本地客户端负责找到原始会话、创建沙箱、启动本地 Codex，并将结果返回给云端 Agent。

第一版用户端不做独立前端，只提供一个 Python 本地客户端。

---

## 二、两类 Agent 和两个运行位置

### 1. 云端沙箱 Agent

云端沙箱 Agent 运行在 Everyone 服务器中。

它只安装和使用 MCP，不安装 Everyone CLI。

云端 Agent 通过 MCP：

- 查询最近的工作总结；
- 查询某个会话的详细总结；
- 查询任务和排期；
- 发起远程求助；
- 等待本地 Agent 的帮助结果；
- 获取本地 Agent 返回的文字和附件。

### 2. 用户本地 Agent

本地 Agent 运行在用户的 Codex、Cursor、Claude Code 等工具中。

本地 Agent 安装标准 Skill，并使用 Everyone CLI。

本地 Agent 通过 CLI：

- 读取用户授权的本地工作会话；
- 生成并上传工作会话总结；
- 获取任务和排期；
- 整理任务完成草稿；
- 整理每日时间去向草稿；
- 在用户确认后正式上传任务完成和时间去向；
- 配合本地客户端处理远程求助。

---

## 三、标准 Skill

需要提供一套标准 Skill，能够安装到 Codex、Cursor、Claude Code 等本地 Agent 工具中。

Skill 负责指导本地 Agent：

- 读取哪些本地工具和聊天记录；
- 如何遵守用户授权范围；
- 如何识别工作会话；
- 如何生成统一格式的工作总结；
- 如何命名大需求和子任务；
- 如何获取任务和排期；
- 如何判断任务是否完成；
- 如何生成任务完成和时间去向草稿；
- 如何先让用户确认，再调用 CLI 正式上传；
- 如何通过 CLI 查询和同步数据；
- 如何配合本地客户端处理远程求助。

Skill 必须采用对应工具支持的标准安装和配置方式。不能依赖只适用于某一个工具的私有实现。

用户必须明确决定：

- 允许读取哪些 AI 工具；
- 允许读取哪些项目或工作区；
- 允许读取哪些会话；
- 是否允许上传工作总结；
- 是否允许本地客户端执行远程协作任务。

系统不能默认读取用户全部聊天记录或全部本地文件。

---

## 四、Everyone CLI

Everyone CLI只安装在用户本地，不安装在云端 Agent 沙箱中。

本地 Agent和本地客户端可以使用这个 CLI 与 Everyone 云端服务通信。

CLI负责本地侧的业务交互，包括：

### 任务和排期

- 查询用户当前任务；
- 查询任务状态、截止时间和关联信息；
- 查询排期；
- 上报任务完成草稿；
- 在用户确认后正式提交任务完成；
- 上传成功后将对应任务直接标记为完成。

### 时间去向

- 整理当天的时间去向；
- 记录每项工作对应的大需求、子任务和工作会话；
- 记录开始时间、结束时间或工作时长；
- 生成时间去向草稿；
- 在用户确认后正式上传时间记录。

### 工作总结

- 上传工作会话总结；
- 查询最近的总结；
- 按关键词查询总结；
- 根据会话 ID获取详细总结；
- 查询相关工作会话。

### 远程协作

Everyone CLI不是云端 Agent 发起求助的工具。

云端 Agent通过 MCP 发起求助。CLI主要由本地 Agent或本地客户端用于：

- 接收或查询求助任务；
- 提交本地 Codex 的回复；
- 上传附件；
- 查询求助执行状态。 （因此创建远程求助时，在工作区中留下的Skill和CLI只专注于这两项能力，没有其他能力，而正常用户本地的安装的Skill中提供的CLI也没有这两项能力）

---

## 五、工作会话

Codex、Cursor、Claude Code 等工具中的一段工作聊天，统一称为一个工作会话。

每个工作会话需要具备：

- 16 位字母和数字组成的唯一 ID；
- 来源工具；
- 大需求名称；
- 子任务名称；
- 会话时间；
- 25 字左右的一句话总结；
- 100 字左右的详细总结；
- 关联任务或排期信息。

### 隐私要求

工作会话默认只上传总结，不上传完整聊天原文。

云端保存：

- 会话 ID；
- 25 字短总结；
- 100 字详细总结；
- 大需求名称；
- 子任务名称；
- 时间；
- 来源工具；
- 关联任务。

完整聊天原文始终保留在用户本地。

---

## 六、需求命名规则

为了支持时间统计和时间穿透，需要统一需求命名。

每个工作会话需要尽量关联：

- 大需求名称；
- 子任务名称。

同一个大需求，在不同会话中要尽量使用完全相同的名称。

同一个子任务，在不同会话中也要尽量使用相同名称。

例如：

```text
大需求：支付系统升级
子任务：支付回调重试
```

多个不同会话都可以归到：

```text
支付系统升级 / 支付回调重试
```

一个大需求可以包含多个子任务。时间穿透图需要支持按照以下层级展开：

```text
大需求
→ 子任务
→ 工作会话
```

如果本地 Agent无法判断名称是否与已有需求一致，应先生成草稿，让用户确认或修改。

---

## 七、任务完成和时间去向的确认流程

任务完成和时间去向都不能由本地 Agent未经用户确认直接上传。

本地 Agent可以通过心跳任务定期整理工作情况。

### 统一流程

```text
本地 Agent检查工作记录
→ 生成拟上传内容
→ 展示给用户
→ 用户确认
→ 本地 Agent调用Everyone CLI
→ 远端接收并正式入账
```

### 任务完成

Agent判断某个任务已经完成后，先生成拟上传内容，包括：

- 对应任务；
- 关联工作会话；
- 完成说明；
- 产出物或附件；
- 是否还有遗留问题。

用户回复“没问题”或等价确认后，本地 Agent才正式上传。

上传成功后：

- 任务直接标记为完成；
- 保存完成说明；
- 关联工作会话；
- 保存相关附件或证据。

### 时间去向

Agent根据当天工作会话生成时间去向草稿，包括：

- 日期；
- 大需求；
- 子任务；
- 工作会话；
- 工作开始和结束时间；
- 工作时长；
- 简短总结；
- 详细总结。

用户确认后正式上传。上传成功后，时间记录进入云端统计。

服务器可以随时接收请求，但本地 Agent必须先得到用户确认，才能提交正式数据。

---

## 八、MCP能力

云端沙箱 Agent通过MCP 使用相关能力。

### 工作总结相关能力

云端 Agent启动时自动获得最近 5 条工作总结。

每条至少包括：

- 会话 ID；
- 25 字短总结；
- 大需求名称；
- 子任务名称；
- 时间信息。

云端 Agent需要更多信息时，可以通过同一个 MCP：

- 获取近期总结列表；
- 按关键词搜索总结；
- 获取某个会话的 100 字详细总结；
- 获取相关会话；
- 获取任务关联信息。

完整会话原文不在云端默认提供，因为原文保留在用户本地。

### 远程求助能力

云端 Agent如果知道某件工作曾经由某个本地 AI 完成，但缺少详细上下文，可以调用另一个远程求助 MCP。

调用时需要提供：

- 目标会话 ID；
- 求助问题；
- 当前任务背景；
- 希望本地 Agent完成的内容；
- 当前云端沙箱中的必要信息。
- 前端向用户说明我们使用了求助能力（这条消息会在飞书中回复用户）

---

## 九、远程求助的阻塞机制

这是远程求助最重要的行为规则。

云端 Agent一旦调用“请求远程帮助”的 MCP：

- 请求会被创建并发送给用户本地客户端；
- 这个 MCP不会立即返回正常结果；
- MCP调用会一直阻塞；
- 在本地 Agent完成处理并提交结果之前，不返回帮助内容。

完整流程：

```text
云端沙箱 Agent
→ 调用 request_remote_help
→ MCP调用保持阻塞
→ Everyone云端创建求助请求
→ 用户本地客户端接收请求
→ 本地客户端还原会话并创建沙箱
→ 启动本地Codex
→ 本地Codex完成工作
→ 本地客户端上传回复和附件
→ request_remote_help 返回回复和附件
→ 云端沙箱 Agent继续工作
```

### 远程求助失败时

如果本地客户端或本地 Codex执行失败，`request_remote_help`可能返回错误结果。


云端 Agent可以根据是超时错误还是其他错误选择调用另一个“获取帮助结果”或继续“等待帮助结果”的 MCP。

这个 MCP的作用不是普通查询，而是用于重新等待已经存在的求助请求。

也就是说：

```text
request_remote_help
→ 一直阻塞等待
→ 如果执行成功，直接返回帮助结果
→ 如果发生错误，MCP报错
→ 云端 Agent可以调用 wait/get_help_result
→ 继续等待同一个求助请求
```


因此需要区分：

- 发起新的远程求助；
- 继续等待已有求助；
- 获取已经完成的求助结果；
- 获取求助失败信息。

如果求助请求仍在执行，等待型 MCP也应该继续阻塞，而不是立刻返回空结果。

---

## 十、远程求助中的本地客户端

第一版本地客户端使用一个 Python 文件实现。

本地客户端负责：

- 和 Everyone 云端建立长连接、轮询或其他通信；
- 接收云端 Agent发来的求助请求；
- 根据会话 ID找到本地完整会话；
- 创建临时工作沙箱；
- 快照原来的工作目录；
- 将选中的完整会话原样放进沙箱；
- 将求助问题和任务说明放进沙箱；
- 将 Everyone CLI和标准 Skill放进沙箱；
- 启动本地 Codex CLI；
- 监听本地 Codex执行结果；
- 接收文字回复和附件；
- 上传回复和附件；
- 将结果关联到原始求助请求。

---

## 十一、会话 ID的本地映射

云端只保存会话总结和会话 ID，完整会话由本地客户端根据 ID还原。

因此本地客户端必须维护：

```text
会话 ID
→ 来源工具
→ 项目或工作区
→ 本地原始会话位置
→ 本地文件路径、数据库位置或会话标识
→ 创建时间和更新时间
```

例如：

```text
7K2F9A1M3Q8R5T6Z
→ Codex
→ 某个项目
→ 本地会话文件或数据库记录
→ 完整原始聊天内容
```

收到远程求助后，本地客户端根据会话 ID找到对应的会话位置，再还原完整上下文。

如果会话已经不存在、位置发生变化或无法读取，本地客户端需要把错误返回给云端求助请求。

---

## 十二、远程求助沙箱

本地客户端为每次远程求助创建独立的临时沙箱。

沙箱中包括：

- 原工作目录的文件快照；
- 被选中的完整工作会话；
- 远程 Agent提交的求助问题；
- 任务书；
- Everyone CLI；
- 标准 Skill；
- 本次协作产生的输出文件。

任务书需要说明：

- 当前背景；
- 求助来源；
- 会话 ID；
- 会话总结；
- 求助问题；
- 期望本地 Codex完成的工作；
- 工作区快照说明；
- CLI和Skill的使用方式；
- 回复要求；
- 附件上传方式。

本地 Codex完成任务后，通过本地 CLI提交：

- 一段文字回复；
- 可选附件；
- 任务完成说明；
- 执行状态。

文字回复作为云端 Agent远程求助 MCP的返回结果。

附件需要同步到发起求助的云端 Agent沙箱中，供它继续查看和使用。

---

## 十三、时间穿透图

云端后台和用户自己的后台都需要展示时间穿透图。

时间穿透图按周查看：

- 可以切换上一周和下一周；
- 可以回到当前周；
- 每天显示总工作时长；
- 每天使用一根柱状图；
- 柱状图内部按工作任务分层；
- 每一层代表一个大需求或子任务；
- 点击某一层可以查看详细工作记录。

展开层级：

```text
某一天
→ 大需求
→ 子任务
→ 工作会话
→ 25 字短总结
→ 100 字详细总结
```

管理员可以查看团队成员的时间穿透。

普通用户可以查看自己的时间穿透。

时间穿透图必须依赖统一的需求名称和会话 ID进行聚合。

---

## 十四、最终架构关系

```text
用户本地 Codex / Cursor / Claude Code
        │
        │ 安装标准 Skill
        │ 使用 Everyone CLI
        ▼
用户本地 Agent
        │
        ▼
用户本地 Python 客户端
        │
        ├── 维护会话 ID与本地原始位置映射
        ├── 执行心跳任务
        ├── 生成任务和时间去向草稿
        ├── 等待用户确认
        ├── 正式上传任务完成和时间去向
        ├── 接收云端求助
        ├── 创建本地沙箱
        └── 启动本地 Codex
        │
        ▼
Everyone 云端服务
        │
        ├── 云端沙箱 Agent
        │      └── 只使用 MCP
        ├── 保存工作会话总结
        ├── 管理任务和排期
        ├── 保存时间去向
        ├── 提供周时间穿透图
        ├── 提供总结查询 MCP
        └── 提供阻塞式远程求助 MCP
```

最终目标是：

> 本地 Agent负责在用户授权范围内识别工作、生成总结、整理任务和时间；用户确认后，才通过本地 Everyone CLI正式上传。云端 Agent只通过 MCP查询这些摘要和任务。当云端 Agent缺少上下文时，通过阻塞式远程求助 MCP唤起用户本地客户端，由本地客户端根据会话 ID还原完整上下文、创建沙箱并启动本地 Codex，最后把回复和附件返回给云端 Agent。